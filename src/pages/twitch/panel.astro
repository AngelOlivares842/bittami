---
import TwitchLayout from '../../layouts/TwitchLayout.astro';
import BentoGrid from '../../components/BentoGrid.jsx';
import Footer from '../../components/Footer.jsx';
---

<TwitchLayout title="BITTAMI | TWITCH PANEL">
  <main class="flex flex-col min-h-screen">
    <div class="twitch-responsive-container">
      {/* CAMBIO CRÍTICO: Usamos client:only="react" para que Astro no 
        inyecte scripts de pre-renderizado que la CSP de Twitch bloquea.
      */}
      <BentoGrid client:only="react" />
    </div>
    
    <div class="mt-auto pb-10 px-6 opacity-50 scale-90 origin-bottom">
      <Footer client:only="react" />
    </div>
  </main>
</TwitchLayout>

<style is:global>
  /* Mantenemos tus estilos de Mekode intactos */
  @media (max-width: 480px) {
    .grid-cols-12, .grid {
      display: flex !important;
      flex-direction: column !important;
      gap: 1.5rem !important;
      padding: 1rem !important;
    }

    .md\:col-span-8, .md\:col-span-4, .md\:col-span-3, .md\:col-span-6 {
      grid-column: span 12 !important;
      width: 100% !important;
      min-height: auto !important;
    }

    h1 {
      font-size: 3.5rem !important;
      line-height: 0.9 !important;
      word-break: break-all;
    }

    .relative.w-48 {
      width: 120px !important;
      margin: 0 auto;
    }
  }

  .violent-shake { animation: shake 0.1s infinite; }
  @keyframes shake {
    0% { transform: translate(1px, 1px); }
    50% { transform: translate(-1px, -2px); }
    100% { transform: translate(1px, -1px); }
  }
</style>